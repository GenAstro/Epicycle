var documenterSearchIndex = {"docs":
[{"category":"section","location":"data_plotting/#Data-Plotting","page":"Data plotting","text":"Data plotting covers everything drawn against axes: time histories, residuals, ground tracks, distributions, and scalar fields over a grid.\n\nThe functions on this page operate on arrays and have no knowledge of where the arrays came from. The examples below therefore build their own data, and they run as written. In practice the arrays usually come from history in AstroCallbacks, but nothing on this page requires it.\n\nAttribute names are Plotly's own and values are passed through unchanged, so Plotly's reference applies directly. This page describes what EpicycleIO adds: how arrays become series, and how attributes are spread across them.","title":"Data Plotting"},{"category":"section","location":"data_plotting/#Series-and-Shapes","page":"Data plotting","text":"xyplot replaces the contents of a panel and xyplot! adds to a panel. Array shapes follow the conventions used by MATLAB and matplotlib: a single array plots against its index, a matrix draws one series per column, and several x/y pairs may be given in one call. Lengths are checked in pairs, so the pairs need have nothing to do with one another.\n\nusing EpicycleIO\n\nt   = collect(0.0:0.25:12.0)\nalt = 400.0 .+ 25.0 .* sin.(t)\n\n# The leading string names a panel. Without one, the default panel is used\nxyplot(\"Altitude\", t, alt; name = \"altitude\", mode = \"lines\", line_color = \"cyan\", line_width = 2)\n\n# plot! adds to a panel rather than replacing it. Lengths need not match\nt2   = collect(0.0:0.5:9.0)\nalt2 = 410.0 .+ 15.0 .* cos.(t2)\nxyplot!(\"Altitude\", t2, alt2; name = \"second spacecraft\")\n\n# One array plots against its index\nxyplot(\"Index\", alt)\n\n# A matrix draws one series per column\nY = hcat(alt, alt .+ 10, alt .- 10)\nxyplot(\"Columns\", t, Y)\n\n# Several pairs in one call, each with its own length\nxyplot(\"Pairs\", t, alt, t2, alt2)\n\nA position column arrives from history as a vector of 3-element vectors rather than a matrix. That shape draws as three series without conversion, which is the case the shape handling exists for.\n\nusing EpicycleIO\n\nt = collect(0.0:0.25:12.0)\n\n# The shape history returns for a vector-valued column: one entry per sample,\n# each holding that sample's components\nr = [[7000.0 + 10i, 100.0i, 1300.0] for i in t]\n\n# Three series, named position[1], position[2] and position[3]\nxyplot(\"Position\", t, r; name = \"position\")\n\n# Name the components when they mean something\nxyplot(\"Position\", t, r; name = [\"radial\", \"along-track\", \"cross-track\"])","title":"Series and Shapes"},{"category":"section","location":"data_plotting/#Attributes-Across-Several-Series","page":"Data plotting","text":"A scalar attribute applies to every series. A vector gives one value per series.\n\nPlotly reads an array on some attributes as one value per data point rather than per series, and marks those attributes arrayOk in its schema. EpicycleIO reads that schema rather than maintaining a list, so the rule is correct for every attribute. On an arrayOk attribute, Plotly's per-point meaning is preserved and per-series is expressed by nesting.\n\nusing EpicycleIO\n\nt = collect(0.0:0.25:12.0)\nr = [[7000.0 + 10i, 100.0i, 1300.0] for i in t]\n\n# One value per series\nxyplot(\"Styled\", t, r; name       = [\"x\", \"y\", \"z\"],\n                     line_color = [\"red\", \"green\", \"blue\"],\n                     line_width = [1, 2, 3])\n\n# marker_size is arrayOk, so a flat vector keeps Plotly's per-point meaning\nxyplot(\"Per point\", t, r; mode = \"markers\", marker_size = [4, 4, 4])\n\n# Nesting always means per series, whatever the schema says\nxyplot(\"Per series\", t, r; mode = \"markers\", marker_size = [[3], [6], [9]])\n\nA vector whose length does not match the series count raises an error rather than recycling.\n\nname is the only attribute that rewrites a value rather than placing it. A scalar name across three series becomes position[1], position[2] and position[3], because three identical legend entries are never what was intended.","title":"Attributes Across Several Series"},{"category":"section","location":"data_plotting/#Gaps","page":"Data plotting","text":"A NaN or an Inf draws as a break in the line rather than raising an error. Gaps are ordinary in this domain: a quantity between ground station passes, argument of periapsis on a circular orbit, or a solver step that did not converge. None of those should cost the user the plot.\n\nusing EpicycleIO\n\nt = collect(0.0:0.5:20.0)\nel = [x < 6 || x > 14 ? NaN : 30.0 + 10.0 * sin(x) for x in t]\n\n# The pass is drawn; the periods with no visibility are gaps rather than zeros\nxyplot(\"Elevation\", t, el; mode = \"lines\", name = \"station 1\")","title":"Gaps"},{"category":"section","location":"data_plotting/#Other-Trace-Types","page":"Data plotting","text":"Each of the following draws the Plotly trace it is named after and has a ! form that adds to a panel rather than replacing it.\n\nFunction Draws\nscatterpolar angle and radius, such as a ground station pass or an antenna pattern\ncontour, heatmap a scalar field over a grid, such as a porkchop plot or a coverage map\nhistogram how a set of values is distributed\nbar a value per category, such as ΔV by maneuver\nband a filled region between two bounds, such as a covariance envelope\nscattergeo longitude and latitude on a map\nscatter3d, surface three axes of data. For a trajectory about a body see 3D plotting\n\nThe table is a snapshot. To list the functions the package actually exports, run names(EpicycleIO).","title":"Other Trace Types"},{"category":"section","location":"data_plotting/#Grids","page":"Data plotting","text":"contour, heatmap and surface take two axes and a matrix. The matrix is indexed [row, column], so rows follow y and columns follow x. A transposed grid is refused rather than drawn sideways.\n\nusing EpicycleIO\n\ndeparture = collect(0.0:2.0:60.0)      # columns\narrival   = collect(100.0:2.0:200.0)   # rows\n\n# C3 is indexed [row, column], so size(C3) == (length(arrival), length(departure))\nC3 = [12.0 + 0.02 * (d - 30)^2 + 0.01 * (a - 150)^2 for a in arrival, d in departure]\n\ncontour(\"Porkchop\", departure, arrival, C3;\n        colorscale = \"Viridis\", colorbar_title = \"C3 km²/s²\")\n\npanel!(\"Porkchop\"; xaxis_title = \"departure, days from epoch\",\n                   yaxis_title = \"arrival, days from epoch\")","title":"Grids"},{"category":"section","location":"data_plotting/#Bands","page":"Data plotting","text":"A band is drawn before the data inside it so the points land on top rather than underneath.\n\nusing EpicycleIO\n\nt     = collect(0.0:0.5:24.0)\nsigma = 0.05 .+ 0.002 .* t\nresid = [0.03 * sin(x) + 0.01 * randn() for x in t]\n\nband(\"Range residuals\", t, -3 .* sigma, 3 .* sigma;\n     name = \"3 sigma\", fillcolor = \"rgba(120,120,120,0.25)\", line_width = 0)\n\nxyplot!(\"Range residuals\", t, resid; mode = \"markers\", name = \"residual\", marker_size = 4)\n\npanel!(\"Range residuals\"; xaxis_title = \"hours\", yaxis_title = \"km\")","title":"Bands"},{"category":"section","location":"data_plotting/#Polar-Plots","page":"Data plotting","text":"A polar plot is drawn the way an operator expects it: north at the top, bearings increasing clockwise, and the zenith at the centre.\n\nTwo of the settings below are correctness rather than preference. Mathematics places zero to the right and increases counterclockwise, while a compass bearing places zero at the top and increases clockwise, so an azimuth drawn in the mathematical convention is mirrored rather than merely rotated. Elevation runs from 90 degrees at the centre to 0 at the rim.\n\nusing EpicycleIO\n\naz = collect(range(0.4, 2.6; length = 40))                 # radians\nel = [deg2rad(75.0) * exp(-((x - 1.5) / 0.7)^2) for x in az]\n\n# thetaunit converts the angle. There is no equivalent for the radial axis, so an\n# elevation in radians against a degree range would place every point on the rim\nscatterpolar(\"Sky view\", az, rad2deg.(el);\n             thetaunit = \"radians\", mode = \"lines\", name = \"Pass 3\")\n\npanel!(\"Sky view\";\n       polar_angularaxis_direction = \"clockwise\",   # compass sense\n       polar_angularaxis_rotation  = 90,            # north at the top\n       polar_radialaxis_range      = [90, 0])       # zenith at the centre","title":"Polar Plots"},{"category":"section","location":"data_plotting/#Traces-Without-a-Wrapper","page":"Data plotting","text":"Plotly has around forty trace types and all of them are reachable. A trace built with PlotlyBase can be handed to xyplot directly, whether or not a wrapper exists for it. A wrapper is a convenience for a common case and never a gate.\n\nusing EpicycleIO\n\nresiduals = randn(200)\n\nxyplot(\"Distribution\", PlotlyBase.violin(y = residuals, box_visible = true, name = \"range\"))","title":"Traces Without a Wrapper"},{"category":"section","location":"data_plotting/#Panel-Coordinate-Families","page":"Data plotting","text":"A panel has a coordinate family because Plotly does. Polar, geographic and three-dimensional traces are drawn on their own subplot types and cannot share a panel with a cartesian trace. Adding a trace from a different family to an existing panel raises an error naming both families rather than silently drawing nothing. This is Plotly's constraint reported rather than a limit imposed here.","title":"Panel Coordinate Families"},{"category":"section","location":"api/#API","page":"API","text":"Every name EpicycleIO exports. For what an attribute does, see Plotly's reference — attribute names here are Plotly's own.\n\nTo see this list from the REPL rather than reading it here:\n\nnames(EpicycleIO)\n\nPages = [\"api.md\"]","title":"API"},{"category":"function","location":"api/#EpicycleIO.band","page":"API","text":"band([panel], x, lower, upper; attributes...)\n\nDraw a filled region between two bounds — a covariance envelope around residuals.\n\nDraw the band before the data that sits inside it, so the points land on top rather than under.\n\nArguments\n\npanel: optional leading string naming the panel.\nx: the horizontal axis, shared by both bounds.\nlower, upper: the two edges, each the same length as x.\n\nNotes\n\nThis is the one wrapper with no Plotly trace behind it. Plotly has no band, so this builds a closed polygon — x forward along the upper bound and back along the lower — and fills it.\n\nReturns\n\nnothing. The panel is drawn on the dashboard.\n\nExamples\n\nband(\"Residuals\", t, -3 .* σ, 3 .* σ; name = \"3σ\", fillcolor = \"rgba(120,120,120,0.25)\")\nxyplot!(\"Residuals\", t, residual; mode = \"markers\", name = \"residual\")\n\n\n\n\n\n","title":"EpicycleIO.band"},{"category":"function","location":"api/#EpicycleIO.band!","page":"API","text":"band!([panel], data...; attributes...)\n\nAdd to a panel instead of replacing what is in it. Takes the same arguments and attributes as band.\n\nReturns\n\nnothing. The panel is redrawn on the dashboard.\n\n\n\n\n\n","title":"EpicycleIO.band!"},{"category":"function","location":"api/#EpicycleIO.bar!","page":"API","text":"bar!([panel], data...; attributes...)\n\nAdd to a panel instead of replacing what is in it. Takes the same arguments and attributes as bar.\n\nReturns\n\nnothing. The panel is redrawn on the dashboard.\n\n\n\n\n\n","title":"EpicycleIO.bar!"},{"category":"function","location":"api/#EpicycleIO.clear!","page":"API","text":"clear!(panel = \"Plot\")\n\nEmpty a panel but leave it on the dashboard.\n\nArguments\n\npanel: the panel to empty. Without one you get the default panel.\n\nNotes\n\nThe layout survives: a panel set to a log axis is still a log axis after clearing, so refilling it does not mean configuring it again.\n\nClearing a panel that does not exist does nothing, silently. Emptying a script's panels before filling them, so a re-run starts fresh rather than accumulating, is an ordinary thing to write — and the first time it runs, those panels are not there yet.\n\nReturns\n\nnothing. The emptied panel is republished, so the browser follows.\n\nExamples\n\nclear!(\"Altitude\")\n\n\n\n\n\n","title":"EpicycleIO.clear!"},{"category":"method","location":"api/#EpicycleIO.clear_all!-Tuple{}","page":"API","text":"clear_all!()\n\nRemove every panel from the dashboard.\n\nclear! empties one panel and leaves it in place; this takes them all away, layout and all, and the next plot starts from an empty page.\n\nNotes\n\nThe browser follows. Forgetting the panels here without saying so would leave the page showing panels that no longer exist, and the next plot would appear among the ghosts.\n\nPanel files left in the served directory are deleted too, all of them rather than only this session's. The directory outlives the process, so otherwise it fills with orphans from every earlier run.\n\nReturns\n\nnothing.\n\nExamples\n\nclear_all!()          # a script that starts from a clean dashboard\n\n\n\n\n\n","title":"EpicycleIO.clear_all!"},{"category":"function","location":"api/#EpicycleIO.contour!","page":"API","text":"contour!([panel], data...; attributes...)\n\nAdd to a panel instead of replacing what is in it. Takes the same arguments and attributes as contour.\n\nReturns\n\nnothing. The panel is redrawn on the dashboard.\n\n\n\n\n\n","title":"EpicycleIO.contour!"},{"category":"function","location":"api/#EpicycleIO.heatmap!","page":"API","text":"heatmap!([panel], data...; attributes...)\n\nAdd to a panel instead of replacing what is in it. Takes the same arguments and attributes as heatmap.\n\nReturns\n\nnothing. The panel is redrawn on the dashboard.\n\n\n\n\n\n","title":"EpicycleIO.heatmap!"},{"category":"function","location":"api/#EpicycleIO.histogram!","page":"API","text":"histogram!([panel], data...; attributes...)\n\nAdd to a panel instead of replacing what is in it. Takes the same arguments and attributes as histogram.\n\nReturns\n\nnothing. The panel is redrawn on the dashboard.\n\n\n\n\n\n","title":"EpicycleIO.histogram!"},{"category":"method","location":"api/#EpicycleIO.open_dashboard-Tuple{Vararg{AbstractString}}","page":"API","text":"open_dashboard()\nopen_dashboard(panel, more...)\n\nOpen the dashboard in a browser tab.\n\nWith no arguments you get every panel. Naming panels opens a tab showing only those, which is how you spread twelve plots across a few windows without splitting where they live:\n\nopen_dashboard(\"Range residuals\", \"Range-rate residuals\")   # navigation, in its own tab\nopen_dashboard(\"Porkchop\")                                  # one plot, full size\n\nThe panels are still one set on one server. A scoped tab is a view of it, so a panel that several tabs show updates in all of them.\n\nArguments\n\ntitles: the panels to show. With none, the tab shows every panel.\n\nNotes\n\nOpens a tab through the operating system's usual handler. If that fails — no browser, or a headless machine — you get a warning carrying the URL rather than an error, because failing to open a window is not a reason to stop a run.\n\nReturns\n\nnothing.\n\n\n\n\n\n","title":"EpicycleIO.open_dashboard"},{"category":"method","location":"api/#EpicycleIO.orbitview!-Tuple","page":"API","text":"orbitview!([panel], sat; kwargs...)\n\nAdd a spacecraft to a view that already exists, keeping what is there.\n\n🔴 Not implemented. Several spacecraft in one view needs the CZML writer to merge entity sets, which it does not yet do.\n\nReturns\n\nNever returns. Throws an ArgumentError naming the way to get the same picture today — give the second spacecraft its own panel with orbitview. It refuses rather than quietly drawing one spacecraft and dropping the other.\n\n\n\n\n\n","title":"EpicycleIO.orbitview!"},{"category":"method","location":"api/#EpicycleIO.orbitview-Tuple","page":"API","text":"orbitview([panel], sat; kwargs...)\n\nDraw a spacecraft's propagated trajectory in three dimensions, with a clock you can play.\n\nThe panel joins the dashboard beside any plots, so a trajectory and the quantities describing it share one page.\n\nEverything is read from the spacecraft:\n\nthe trajectory, from sat.history\nthe epoch, from the earliest recorded sample\none coloured arc per propagation segment, from the same palette the plots use\nthe name shown on the entity, from sat.name\n\npropagate!(prop, sat, StopAt(sat, PropDurationDays(), 1.0))\n\norbitview(sat)\norbitview(\"Mission\", sat)          # name the panel\n\nArguments\n\npanel: optional leading string naming the panel.\nsat: the spacecraft. It must have been propagated — the trajectory is read from its history.\n\nKeyword arguments\n\ncolors: palette for the arcs, cycled. Defaults to the shared palette, so a trajectory and the plots of it match.\nlinewidth: arc width in pixels.\nspeed: initial playback rate, in simulated seconds per real second. :auto fits the whole trajectory into about two minutes.\n\nNotes\n\nPositions go to the browser in the inertial frame and Cesium applies the rotation to fixed itself, so the orbit holds still against the stars and the body turns beneath it. The epoch is converted to UTC on the way out, whatever scale the history was recorded in, because Cesium reads times as UTC.\n\nAn impulsive burn is marked with a point labelled with its ΔV, differenced from the velocities either side of it rather than taken from whatever commanded it.\n\nEarth is the only central body today.\n\nReturns\n\nnothing. The view is drawn on the dashboard.\n\nThrows an ArgumentError if the spacecraft has no propagated history, or if nothing in that history is long enough to draw an arc.\n\n\n\n\n\n","title":"EpicycleIO.orbitview"},{"category":"function","location":"api/#EpicycleIO.panel!","page":"API","text":"panel!([panel]; layout...)\npanel!([panel], layout::PlotlyBase.Layout)\n\nSet a panel's titles, axis labels, scales and limits.\n\nArguments\n\npanel: the panel to configure. Without one you get the default panel.\nlayout...: Plotly layout attributes. Nested settings are spelled with underscores, so yaxis_type reaches Plotly as yaxis.type.\n\nNotes\n\nSettings accumulate rather than replace, so calling this twice for the same axis keeps both. Limits are an ordered pair, so an axis runs backwards if you give them backwards. A Dict is not a way in — write legend_orientation = \"h\", not legend = Dict(\"orientation\" => \"h\").\n\nConfiguring a panel that does not exist yet is allowed and creates it empty, which is what lets you set a panel up before plotting into it. A name that was meant to reach an existing panel and does not earns a warning naming the panels there are.\n\nReturns\n\nnothing. The panel is republished with the new layout.\n\nExamples\n\npanel!(\"Altitude\"; xaxis_title = \"hours from epoch\", yaxis_title = \"km\", yaxis_type = \"log\")\n\npanel!(\"Decay\"; xaxis_range = [6, 0])            # runs backwards\n\n\n\n\n\n","title":"EpicycleIO.panel!"},{"category":"method","location":"api/#EpicycleIO.report-Tuple{AbstractString}","page":"API","text":"report(path; columns...)\n\nWrite named columns to a delimited text file.\n\nThe keywords become the column headings, so they are your words rather than whatever the quantity happens to be called — someone who did not write the run has to read this. A vector-valued column expands into components, so position becomes position_1, position_2, position_3.\n\nreport(\"flight.txt\"; time = [x.jd for x in t], position = r, velocity = v)\n\nArguments\n\npath: the file to write. An existing file is overwritten.\ndelim: what separates the columns. Two spaces by default.\ncolumns...: the data, one keyword per column. Every column must be the same length.\n\nNotes\n\nColumns carry no units of their own — they are written as given, so name the keyword for what the number is (altitude_km) if the reader will need to know.\n\nValues must render on one line. An epoch does not, so convert it first — t.jd, t.mjd or t.isot — and the file then records which scale and format you meant.\n\nReturns\n\nThe path written, so it can be passed straight on to something that reads it.\n\nThrows an ArgumentError if no columns are given, if the columns differ in length, or if a value's printed form spans lines and would break a row.\n\n\n\n\n\n","title":"EpicycleIO.report"},{"category":"function","location":"api/#EpicycleIO.scatter3d!","page":"API","text":"scatter3d!([panel], data...; attributes...)\n\nAdd to a panel instead of replacing what is in it. Takes the same arguments and attributes as scatter3d.\n\nReturns\n\nnothing. The panel is redrawn on the dashboard.\n\n\n\n\n\n","title":"EpicycleIO.scatter3d!"},{"category":"function","location":"api/#EpicycleIO.scattergeo!","page":"API","text":"scattergeo!([panel], data...; attributes...)\n\nAdd to a panel instead of replacing what is in it. Takes the same arguments and attributes as scattergeo.\n\nReturns\n\nnothing. The panel is redrawn on the dashboard.\n\n\n\n\n\n","title":"EpicycleIO.scattergeo!"},{"category":"function","location":"api/#EpicycleIO.scatterpolar!","page":"API","text":"scatterpolar!([panel], data...; attributes...)\n\nAdd to a panel instead of replacing what is in it. Takes the same arguments and attributes as scatterpolar.\n\nReturns\n\nnothing. The panel is redrawn on the dashboard.\n\n\n\n\n\n","title":"EpicycleIO.scatterpolar!"},{"category":"function","location":"api/#EpicycleIO.surface!","page":"API","text":"surface!([panel], data...; attributes...)\n\nAdd to a panel instead of replacing what is in it. Takes the same arguments and attributes as surface.\n\nReturns\n\nnothing. The panel is redrawn on the dashboard.\n\n\n\n\n\n","title":"EpicycleIO.surface!"},{"category":"method","location":"api/#EpicycleIO.xyplot!-Tuple","page":"API","text":"xyplot!([panel], x, y; attributes...)\n\nAdd a line to a panel instead of replacing what is there. Takes everything xyplot does.\n\nLines need not be the same length, so two spacecraft sampled differently share a panel without any fuss.\n\nSafe to call from inside a propagation or a solver callback: writes are throttled and happen on their own task, so publishing cannot slow the run feeding it. A zoom you set survives the update, where xyplot resets the view because the data underneath it changed.\n\nReturns\n\nnothing. The panel is redrawn on the dashboard.\n\nExamples\n\nxyplot(\"Altitude\",  t_a, alt_a; name = \"Sat A\")\nxyplot!(\"Altitude\", t_b, alt_b; name = \"Sat B\")\n\n\n\n\n\n","title":"EpicycleIO.xyplot!"},{"category":"method","location":"api/#EpicycleIO.xyplot-Tuple","page":"API","text":"xyplot([panel], x, y; attributes...)\nxyplot([panel], y; attributes...)\n\nDraw a line, replacing whatever was in the panel.\n\nThe leading string names a panel; without one you get the default panel. Plotting into a name that already exists replaces it, so re-running a script gives you one figure rather than twenty. Use xyplot! to add instead.\n\nAttributes are Plotly's, passed through untouched:\n\nxyplot(t, altitude; name = \"altitude\", mode = \"lines\", line_color = \"cyan\", line_width = 2)\n\nA matrix or a vector of vectors draws one line per component, which is what a position column from history already is:\n\nxyplot(t, r; name = [\"x\", \"y\", \"z\"], line_color = [\"red\", \"green\", \"blue\"])\n\nArguments\n\npanel: optional leading string naming the panel.\nx, y: paired arrays. Several pairs may follow one another, each with its own length. Given y alone, the horizontal axis is its index.\n\nNotes\n\nx and y are plain arrays and carry no units, frame or time system of their own — whatever you computed them in is what gets drawn, so label the axes with panel!. A Time is not a number: render it first, or subtract the epoch to get hours.\n\nA NaN or an Inf draws as a break in the line rather than raising.\n\nReturns\n\nnothing. The panel is drawn on the dashboard, and a browser tab opens if none is watching.\n\n\n\n\n\n","title":"EpicycleIO.xyplot"},{"category":"function","location":"api/#PlotlyBase.bar","page":"API","text":"bar([panel], categories, values; attributes...)\n\nDraw one bar per category — ΔV by maneuver, passes by station.\n\nArguments\n\npanel: optional leading string naming the panel.\ncategories: the label for each bar. Strings or numbers.\nvalues: bar heights, one per category.\n\nReturns\n\nnothing. The panel is drawn on the dashboard.\n\nExamples\n\nbar(\"ΔV budget\", [\"TOI\", \"MCC\", \"MOI\"], [2.82, 1.14, 0.48])\npanel!(\"ΔV budget\"; yaxis_title = \"km/s\")\n\n\n\n\n\n","title":"PlotlyBase.bar"},{"category":"function","location":"api/#PlotlyBase.contour","page":"API","text":"contour([panel], x, y, Z; attributes...)\ncontour([panel], Z; attributes...)\n\nDraw a scalar field over a grid, as labelled contour lines — a porkchop plot, a coverage map.\n\nArguments\n\npanel: optional leading string naming the panel.\nx: the horizontal axis, one value per column of Z.\ny: the vertical axis, one value per row of Z.\nZ: the field, indexed [row, column], so size(Z) == (length(y), length(x)).\n\nGiven Z alone, the axes are its indices.\n\nNotes\n\nRows follow y and columns follow x. That is Plotly's convention, and it is why the horizontal axis is the second index rather than the first — the shape is checked, so a transposed grid is refused rather than drawn sideways.\n\nReturns\n\nnothing. The panel is drawn on the dashboard.\n\nExamples\n\ncontour(\"Porkchop\", departure_days, arrival_days, C3;\n         colorscale = \"Viridis\", colorbar_title = \"C3 km²/s²\")\n\n\n\n\n\n","title":"PlotlyBase.contour"},{"category":"function","location":"api/#PlotlyBase.heatmap","page":"API","text":"heatmap([panel], x, y, Z; attributes...)\nheatmap([panel], Z; attributes...)\n\nDraw a scalar field over a grid, as coloured cells — a porkchop plot, a coverage map.\n\nArguments\n\npanel: optional leading string naming the panel.\nx: the horizontal axis, one value per column of Z.\ny: the vertical axis, one value per row of Z.\nZ: the field, indexed [row, column], so size(Z) == (length(y), length(x)).\n\nGiven Z alone, the axes are its indices.\n\nNotes\n\nRows follow y and columns follow x. That is Plotly's convention, and it is why the horizontal axis is the second index rather than the first — the shape is checked, so a transposed grid is refused rather than drawn sideways.\n\nReturns\n\nnothing. The panel is drawn on the dashboard.\n\nExamples\n\nheatmap(\"Porkchop\", departure_days, arrival_days, C3;\n         colorscale = \"Viridis\", colorbar_title = \"C3 km²/s²\")\n\n\n\n\n\n","title":"PlotlyBase.heatmap"},{"category":"function","location":"api/#PlotlyBase.histogram","page":"API","text":"histogram([panel], values; attributes...)\n\nDraw how a set of values is distributed — residuals, Monte Carlo misses.\n\nPlotly chooses the bins. Set nbinsx for an upper bound on their number, or xbins_size to fix the width.\n\nArguments\n\npanel: optional leading string naming the panel.\nvalues: the sample. One array, not a pair.\n\nReturns\n\nnothing. The panel is drawn on the dashboard.\n\nExamples\n\nhistogram(\"Range residuals\", residuals; nbinsx = 40)\n\n\n\n\n\n","title":"PlotlyBase.histogram"},{"category":"function","location":"api/#PlotlyBase.scatter3d","page":"API","text":"scatter3d([panel], x, y, z; attributes...)\n\nDraw a line or points in three axes of data — a B-plane, a parameter sweep.\n\nThis is a data plot with three axes, not a view of a body. For a trajectory around a central body, with a clock and a globe, use orbitview.\n\nArguments\n\npanel: optional leading string naming the panel.\nx, y, z: one value each per point, all the same length.\n\nReturns\n\nnothing. The panel is drawn on the dashboard.\n\nExamples\n\nscatter3d(\"B-plane\", b_dot_r, b_dot_t, epoch_offset; mode = \"markers\")\n\n\n\n\n\n","title":"PlotlyBase.scatter3d"},{"category":"function","location":"api/#PlotlyBase.scattergeo","page":"API","text":"scattergeo([panel], longitude, latitude; attributes...)\n\nDraw a track on a map of the body — a ground track, a station network.\n\nArguments\n\npanel: optional leading string naming the panel.\nlongitude: degrees east, in the body-fixed frame you computed them in.\nlatitude: degrees north, same frame.\n\nNotes\n\nDegrees, not radians, and geodetic rather than geocentric latitude if you want the track to sit on the coastlines the way a map expects. The projection and the coastlines are Plotly's, set through panel! with geo_ attributes.\n\nReturns\n\nnothing. The panel is drawn on the dashboard.\n\nExamples\n\nscattergeo(\"Ground track\", lon_deg, lat_deg; mode = \"lines\", name = \"Explorer\")\npanel!(\"Ground track\"; geo_projection_type = \"equirectangular\", geo_showland = true)\n\n\n\n\n\n","title":"PlotlyBase.scattergeo"},{"category":"function","location":"api/#PlotlyBase.scatterpolar","page":"API","text":"scatterpolar([panel], θ, r; attributes...)\n\nDraw against angle and radius — a ground station pass, an antenna pattern.\n\nArguments\n\npanel: optional leading string naming the panel. Without one you get the default panel.\nθ: angle, in degrees unless you pass thetaunit = \"radians\".\nr: radius, in whatever units the radial axis is set to.\n\nNotes\n\nthetaunit converts the angle for you. There is no equivalent for the radial axis, so an elevation in radians plotted against a degree range puts every point on the rim — convert it yourself. A polar trace cannot share a panel with a cartesian one; Plotly draws them on different subplot types.\n\nReturns\n\nnothing. The panel is drawn on the dashboard.\n\nExamples\n\nscatterpolar(\"Sky view\", azimuth, rad2deg.(elevation); mode = \"lines\", name = \"Pass 3\")\n\npanel!(\"Sky view\";\n       polar_angularaxis_direction = \"clockwise\",   # compass sense\n       polar_angularaxis_rotation  = 90,            # north at the top\n       polar_radialaxis_range      = [90, 0])       # zenith at the centre\n\n\n\n\n\n","title":"PlotlyBase.scatterpolar"},{"category":"function","location":"api/#PlotlyBase.surface","page":"API","text":"surface([panel], x, y, Z; attributes...)\nsurface([panel], Z; attributes...)\n\nDraw a scalar field over a grid, as a surface in three axes — a porkchop plot, a coverage map.\n\nArguments\n\npanel: optional leading string naming the panel.\nx: the horizontal axis, one value per column of Z.\ny: the vertical axis, one value per row of Z.\nZ: the field, indexed [row, column], so size(Z) == (length(y), length(x)).\n\nGiven Z alone, the axes are its indices.\n\nNotes\n\nRows follow y and columns follow x. That is Plotly's convention, and it is why the horizontal axis is the second index rather than the first — the shape is checked, so a transposed grid is refused rather than drawn sideways.\n\nReturns\n\nnothing. The panel is drawn on the dashboard.\n\nExamples\n\nsurface(\"Porkchop\", departure_days, arrival_days, C3;\n         colorscale = \"Viridis\", colorbar_title = \"C3 km²/s²\")\n\n\n\n\n\n","title":"PlotlyBase.surface"},{"category":"section","location":"reporting/#Reporting","page":"Reporting","text":"report writes named columns to a delimited text file: the same arrays that would be plotted, in a form that can be read, diffed, or loaded by something else.\n\nLike the plotting functions, report operates on arrays and has no knowledge of where they came from, so the examples below build their own data and run as written.\n\nusing EpicycleIO\n\nt = collect(0.0:0.25:6.0)\nr = [[7000.0 + 10i, 100.0i, 1300.0] for i in t]\nv = [[0.0, 7.35 - 0.001i, 1.0] for i in t]\n\nreport(\"flight.txt\"; time = t, position = r, velocity = v)","title":"Reporting"},{"category":"section","location":"reporting/#Column-Headings","page":"Reporting","text":"The columns are named by the keywords given, rather than by whatever the quantity is called internally. A report is usually read by someone who did not perform the run, and time tells that reader more than epoch does.\n\nA vector-valued column expands into one column per component:\n\ntime        position_1    position_2    position_3    velocity_1  …\n\nColumns must be the same length. Columns from two different recordings have their own sample times and cannot share a report, so a length mismatch raises an error naming the columns and their lengths rather than writing a file that silently misaligns.\n\nColumns carry no units of their own. They are written as given, so a keyword should name what the number is when the reader will need to know.\n\nusing EpicycleIO\n\nt   = collect(0.0:0.25:6.0)\nalt = 400.0 .+ 25.0 .* sin.(t)\n\nreport(\"flight.txt\"; hours_from_epoch = t, altitude_km = alt)","title":"Column Headings"},{"category":"section","location":"reporting/#Epochs-Are-Rendered-by-the-Caller","page":"Reporting","text":"report does not interpret epochs, and this is deliberate. A bare Julian date does not record whether it is TT or UTC, and a text writer is the wrong place to make that decision. A Time passed directly raises an error naming the conversions available.\n\nusing Epicycle\nusing EpicycleIO\n\nt = [Time(2458849.5 + 0.01k, 0.0, :tdb, :jd) for k in 0:5]\nalt = 400.0 .+ 25.0 .* collect(0.0:5.0)\n\n# Choose the scale and format, and the file then records which was meant\nreport(\"flight.txt\"; time = [x.jd   for x in t], altitude = alt)\nreport(\"flight.txt\"; time = [x.isot for x in t], altitude = alt)\n\nThe same rule catches anything else that would break a row. A value whose printed form spans several lines would turn one row into several without any error, so report refuses it and says what to convert.","title":"Epochs Are Rendered by the Caller"},{"category":"section","location":"reporting/#The-Same-Data,-Reported-and-Plotted","page":"Reporting","text":"Both take the arrays from one history call. There the resemblance stops, and it should. A file has column headings that must be called something, so report names them with keywords. A plot has series, which Plotly names with name. The two are alike in taking the same data, not in their syntax.\n\n<!-- doc-fragment -->\n\nt, r = history(Calc(epoch, sat), Calc(position_vector, sat, EarthMJ2000Eq))\n\nreport(\"flight.txt\"; time = [x.jd for x in t], position = r)\nxyplot(\"Position\", t, r; name = [\"x\", \"y\", \"z\"])","title":"The Same Data, Reported and Plotted"},{"category":"section","location":"dashboard/#The-Dashboard","page":"Dashboard","text":"Your plots appear in a browser page served from your own machine. The page stays open while you keep working and updates as you plot, including from inside a running solve. A 3D view is a panel on the same page, so a trajectory and the quantities describing it sit side by side rather than in separate windows.\n\nThe first plot opens a tab. Later plots go to the same one.","title":"The Dashboard"},{"category":"section","location":"dashboard/#Naming-Panels","page":"Dashboard","text":"A leading string names a panel. Without one you get the default panel, which is fine for a quick look and awkward once you have more than one thing to see.\n\nPlotting into a name that already exists replaces that panel. Run your script twice and you have one figure, not two. Use xyplot! when you mean to add.\n\nusing EpicycleIO\n\nt   = collect(0.0:0.25:12.0)\nalt = 400.0 .+ 25.0 .* sin.(t)\n\nxyplot(\"Altitude\", t, alt; name = \"Sat A\")\nxyplot!(\"Altitude\", t, alt .+ 30; name = \"Sat B\")   # adds\nxyplot(\"Altitude\", t, alt .- 30; name = \"Sat C\")    # replaces both","title":"Naming Panels"},{"category":"section","location":"dashboard/#Titles,-Axes-and-Limits","page":"Dashboard","text":"panel! sets the layout. Its keywords go to Plotly's layout, so Plotly's reference applies. Nested settings are written with underscores rather than as a Dict: yaxis_type reaches Plotly as yaxis.type, and legend_orientation = \"h\" is how you set a nested legend option.\n\nSettings accumulate, so calling panel! twice for the same axis keeps both. Limits are an ordered pair, so an axis runs backwards if you give them backwards.\n\nusing EpicycleIO\n\nt   = collect(0.0:0.25:12.0)\nalt = 400.0 .+ 25.0 .* sin.(t)\n\nxyplot(\"Altitude\", t, alt)\n\npanel!(\"Altitude\";\n       title       = \"Altitude above the ellipsoid\",\n       xaxis_title = \"hours from epoch\",\n       yaxis_title = \"km\",\n       yaxis_type  = \"log\")\n\nxyplot(\"Decay\", t, 500.0 .- 8.0 .* t)\npanel!(\"Decay\"; xaxis_range = [12, 0])     # runs backwards\n\nConfiguring a panel before plotting into it is allowed and creates it empty. If you name a panel that does not exist and did not mean to create one, you get a warning listing the panels that do.","title":"Titles, Axes and Limits"},{"category":"section","location":"dashboard/#Too-Many-Panels","page":"Dashboard","text":"Twelve panels in one tab is unusable. There are two ways out.\n\nThe grid and tabs control at the top right of the page switches between showing every panel at once and showing one at a time with a strip of names.\n\nOr open a tab holding only the panels you name:\n\n<!-- doc-fragment -->\n\nopen_dashboard(\"Range residuals\", \"Range-rate residuals\")   # navigation, its own window\nopen_dashboard(\"Porkchop\")                                  # one plot, full size\nopen_dashboard()                                            # everything, as before\n\nThe panels are still one set. A tab showing a subset is a view of it, so a panel that two tabs both show updates in both.","title":"Too Many Panels"},{"category":"section","location":"dashboard/#Watching-a-Run","page":"Dashboard","text":"xyplot! is safe to call from inside a propagation or a solver callback. Writes are throttled and happen on their own task, so publishing cannot slow the run that feeds it. Zoom into a panel while this runs and the view stays where you put it, because updates redraw in place rather than resetting what you set.\n\nusing EpicycleIO\n\n# What a solver callback would do, one iteration at a time\nfor k in 1:30\n    cost      = 10.0 / k\n    violation = 1.0 / k^2\n    xyplot!(\"Convergence\", [Float64(k)], [cost];      mode = \"markers\", name = \"cost\")\n    xyplot!(\"Feasibility\", [Float64(k)], [violation]; mode = \"markers\", name = \"max violation\")\nend\n\nOne xyplot! per point makes one series per call, which is fine for a few hundred and slows the browser well before a few thousand. If you already hold the whole history, plot it once as a growing series instead.","title":"Watching a Run"},{"category":"section","location":"dashboard/#Clearing-and-Closing","page":"Dashboard","text":"using EpicycleIO\n\nt = collect(0.0:0.25:12.0)\nxyplot(\"Altitude\", t, 400.0 .+ 25.0 .* sin.(t))\n\nclear!(\"Altitude\")     # empty one panel, keeping the layout you set\nclear_all!()           # remove every panel\nEpicycleIO.close_dashboard()      # stop the server; the page stops updating\n\nClosing the browser tab changes nothing in Julia. Your panels live in Julia rather than in the page, so plotting again opens a new tab and picks up where the last one left off.\n\nTo stop plots opening tabs at all, in a script or a test:\n\nusing EpicycleIO\n\nEpicycleIO.auto_open!(false)","title":"Clearing and Closing"},{"category":"section","location":"dashboard/#What-Is-Actually-Happening","page":"Dashboard","text":"Julia writes each panel to a file and a small local server hands those files to the page, which polls for changes. Nothing leaves your machine, and no account or key is needed anywhere.\n\nThe drawing libraries are the one exception. The page loads Plotly and Cesium from a public CDN at exact pinned versions, each the first time it is needed. Plotly arrives when the page first opens. Cesium arrives only when you first render a trajectory, so a session that plots data never fetches it at all. Together they come to about 10 MB.\n\nYour browser then caches both for a week. The cache is on disk rather than per session, so new tabs, new Julia sessions, a browser restart and a reboot all reuse it. When the week is up your browser asks whether the file has changed and is told it has not, which costs a few bytes instead of another download, and because the versions are pinned that answer does not change.\n\nSo you need a network connection the first time you plot on a given browser, and after that you do not. On a machine that cannot reach the internet at all, the panel stays blank. It will not tell you why, so if a panel never draws on a restricted network, this is the first thing to check.","title":"What Is Actually Happening"},{"category":"section","location":"#EpicycleIO","page":"Home","text":"The EpicycleIO module provides plotting, reporting, and three-dimensional trajectory visualization for Epicycle results. Plots are drawn by Plotly.js and trajectories by Cesium, both in a browser page served from the local machine. The page updates while a script is still running, so a propagation or a solver iteration can be watched as it proceeds.\n\nThe plotting and reporting functions take arrays, and have no knowledge of spacecraft, states or coordinate systems. history in AstroCallbacks produces the arrays. Data from two spacecraft needs no special handling: the two recordings are two pairs of arrays with their own lengths.\n\nThe three-dimensional view is the exception. orbitview takes a Spacecraft and reads the reference frame and the epoch from it. Earth is the only central body supported for these views, and the body comes from the frame the history was recorded in.\n\nAttribute names are Plotly's own, such as name, mode, line_color and marker_size, and values pass through unchanged. Plotly's reference documentation applies directly to this interface.\n\nReferences:\n\nPlotly JavaScript reference, https://plotly.com/javascript/reference/\nCesiumJS, https://cesium.com/platform/cesiumjs/\nCZML specification, https://github.com/AnalyticalGraphicsInc/czml-writer/wiki","title":"EpicycleIO"},{"category":"section","location":"#Installation","page":"Home","text":"EpicycleIO is registered in the Gen Astro registry rather than Julia's General registry, because it is released under the Gen Astro Source Available License. Add the registry once, then install as usual:\n\nusing Pkg\nPkg.Registry.add(RegistrySpec(url = \"https://github.com/GenAstro/GenAstro.git\"))\nPkg.add(\"EpicycleIO\")\n\nGeneral is still required, since these packages depend on packages registered there.","title":"Installation"},{"category":"section","location":"#Quick-Start","page":"Home","text":"The Epicycle umbrella does not re-export xyplot; the name collides with Plots.jl and Makie. Load EpicycleIO explicitly. The example below propagates a spacecraft, draws the trajectory, plots two quantities describing it, and writes the same data to a file. It runs as written.\n\nusing Epicycle\nusing EpicycleIO\n\nsat = Spacecraft(state = CartesianState([7000.0, 0.0, 1300.0, 0.0, 7.35, 1.0]),\n                 time  = Time(\"2020-01-01T00:00:00.000\", UTC(), ISOT()),\n                 name  = \"Explorer\")\n\nprop = OrbitPropagator(ForceModel(PointMassGravity(earth, ())),\n                       IntegratorConfig(DP8(); abstol = 1e-12, reltol = 1e-12, dt = 60.0))\n\npropagate!(prop, sat, StopAt(sat, PropDurationDays(), 0.25))\n\n# The trajectory. orbitview takes the spacecraft; the frame and epoch come from it\norbitview(\"Trajectory\", sat)\n\n# Columns out of the recorded history, one walk for all of them\nt, r, rmag = history(Calc(epoch,              sat),\n                     Calc(position_vector,    sat, EarthMJ2000Eq),\n                     Calc(position_magnitude, sat))\n\n# Time is not a number. Subtract the epoch before plotting against it\nhours = [(x - first(t)) * 24 for x in t]\n\n# The leading string names a panel; a browser tab opens on the first plot\nxyplot(\"Radius\", hours, rmag; name = \"|r|\", line_color = \"cyan\")\n\n# Nested Plotly settings are spelled with underscores, so yaxis_title becomes yaxis.title\npanel!(\"Radius\"; xaxis_title = \"hours from epoch\", yaxis_title = \"km\")\n\n# A position column is a vector of 3-element vectors and draws as three series\nxyplot(\"Position\", hours, r; name = [\"x\", \"y\", \"z\"])\n\n# The same arrays as a file. The keywords become the column headings\nreport(\"flight.txt\"; time = [x.jd for x in t], position = r, radius = rmag)\n\"flight.txt\"\n\nThe API is documented with docstrings; Plotly's own documentation is not duplicated here. In the REPL, type ? to enter help mode, then enter a name. ?xyplot gives the shape rules and how attributes are distributed across several series, and ?orbitview gives what is read from a spacecraft. names(EpicycleIO) lists every exported name.","title":"Quick Start"},{"category":"section","location":"#Data-Plotting","page":"Home","text":"xyplot draws lines and markers and replaces whatever the panel held, and xyplot! adds to a panel. Other functions draw other Plotly traces and take the name of the trace they draw. Every one has a ! form.\n\nArray shapes follow the conventions used by MATLAB and matplotlib. A single array plots against its index, and a matrix draws one series per column. Several x/y pairs may be given in one call, and lengths are checked pairwise.\n\nusing EpicycleIO\n\nt   = collect(0.0:0.25:12.0)\nalt = 400.0 .+ 25.0 .* sin.(t)\nr   = [[7000.0 + 10i, 100.0i, 1300.0] for i in t]\n\n# Several forms of the same call\nxyplot(\"A\", alt)                            # index on the horizontal axis\nxyplot(\"B\", t, alt)                         # one series\nxyplot(\"C\", t, hcat(alt, alt .+ 10))        # one series per column of a matrix\nxyplot(\"D\", t, alt, t[1:20], alt[1:20])     # two series, independent lengths\n\n# A scalar attribute applies to every series; a vector gives one value per series\nxyplot(\"E\", t, r; name       = [\"radial\", \"along-track\", \"cross-track\"],\n                line_color = [\"red\", \"green\", \"blue\"],\n                line_width = [1, 2, 3])\n\n# Other trace types take their data positionally and pass attributes through\nhistogram(\"Residuals\", randn(200); nbinsx = 40)\n\n# Any Plotly trace can be built directly and handed over, whether or not a wrapper exists\nxyplot(\"Distribution\", PlotlyBase.violin(y = randn(200), box_visible = true, name = \"range\"))\n\nA NaN or an Inf draws as a break in the line. Gaps are ordinary in this domain: a quantity between ground station passes, argument of periapsis on a circular orbit, a solver step that did not converge.","title":"Data Plotting"},{"category":"section","location":"#Three-Dimensional-Views","page":"Home","text":"orbitview draws a propagated trajectory about the central body, with a clock that can be played, paused and run at a chosen rate. The view is a panel on the same page as the plots, so a trajectory and the quantities describing it are seen together.\n\nEverything the view needs comes from the spacecraft: the trajectory from its recorded history, the epoch from the earliest sample, one coloured arc per propagation segment, and the label from its name. Each impulsive maneuver is marked with a point labelled with its magnitude, computed from the velocity change across the maneuver.\n\n<!-- doc-fragment -->\n\n# Takes the spacecraft, not arrays. See the Quick Start above for the setup\norbitview(\"Trajectory\", sat)\n\n# Playback defaults to fitting the trajectory into about two minutes of real time\norbitview(\"Trajectory\", sat; speed = 3600)     # one hour of flight per second\n\nThe camera is fixed in the inertial frame, so the orbit holds still against the stars and the central body rotates beneath it. A view holds one spacecraft, and a second spacecraft takes its own panel.","title":"Three-Dimensional Views"},{"category":"section","location":"#Reporting","page":"Home","text":"report writes named columns to a delimited text file. The keywords become the column headings, and a vector-valued column expands into one column per component. Columns must be the same length, so columns from two different recordings cannot share a report.\n\nusing EpicycleIO\n\nt = collect(0.0:0.25:6.0)\nr = [[7000.0 + 10i, 100.0i, 1300.0] for i in t]\nv = [[0.0, 7.35 - 0.001i, 1.0] for i in t]\n\nreport(\"flight.txt\"; time = t, position = r, velocity = v)\n\"flight.txt\"\n\nreport does not interpret epochs, since a bare Julian date does not record whether it is TT or UTC. A Time passed directly raises an error, naming t.jd, t.mjd and t.isot as the conversions available.","title":"Reporting"},{"category":"section","location":"#The-Dashboard","page":"Home","text":"A leading string names a panel, and plotting into a name that already exists replaces that panel, so a script run twice leaves one figure rather than two. A control at the top right of the page switches between showing every panel at once and showing one panel at a time.\n\nusing EpicycleIO\n\nt = collect(0.0:0.25:12.0)\nxyplot(\"Altitude\", t, 400.0 .+ 25.0 .* sin.(t))\nxyplot(\"Radius\",   t, 7000.0 .+ 10.0 .* t)\n\nopen_dashboard(\"Altitude\")    # a tab showing named panels only\nclear!(\"Altitude\")            # empty one panel, leaving its layout\nclear_all!()                  # remove every panel\nEpicycleIO.close_dashboard()             # stop the server; the page stops updating\n\nEpicycleIO.auto_open!(false)  # stop plots opening tabs, for a script or a test\nfalse\n\nPlotly and Cesium are downloaded the first time they are needed and then cached by the browser, so the first plot on a machine with no network connection will not draw. Nothing else leaves the local machine, and no account or key is required.","title":"The Dashboard"},{"category":"section","location":"#Reference","page":"Home","text":"The names a reader works through most. Every exported name is on the API page.","title":"Reference"},{"category":"section","location":"#Where-to-go-next","page":"Home","text":"Data plotting, what can be drawn, styling, and grids\n3D plotting, trajectories about a central body, with a clock\nThe dashboard, panels, live update, and keeping plots where they are wanted\nReporting, columns, headings, and what a value may be\nAPI, every exported name","title":"Where to go next"},{"category":"function","location":"#EpicycleIO.xyplot","page":"Home","text":"xyplot([panel], x, y; attributes...)\nxyplot([panel], y; attributes...)\n\nDraw a line, replacing whatever was in the panel.\n\nThe leading string names a panel; without one you get the default panel. Plotting into a name that already exists replaces it, so re-running a script gives you one figure rather than twenty. Use xyplot! to add instead.\n\nAttributes are Plotly's, passed through untouched:\n\nxyplot(t, altitude; name = \"altitude\", mode = \"lines\", line_color = \"cyan\", line_width = 2)\n\nA matrix or a vector of vectors draws one line per component, which is what a position column from history already is:\n\nxyplot(t, r; name = [\"x\", \"y\", \"z\"], line_color = [\"red\", \"green\", \"blue\"])\n\nArguments\n\npanel: optional leading string naming the panel.\nx, y: paired arrays. Several pairs may follow one another, each with its own length. Given y alone, the horizontal axis is its index.\n\nNotes\n\nx and y are plain arrays and carry no units, frame or time system of their own — whatever you computed them in is what gets drawn, so label the axes with panel!. A Time is not a number: render it first, or subtract the epoch to get hours.\n\nA NaN or an Inf draws as a break in the line rather than raising.\n\nReturns\n\nnothing. The panel is drawn on the dashboard, and a browser tab opens if none is watching.\n\n\n\n\n\n","title":"EpicycleIO.xyplot"},{"category":"function","location":"#EpicycleIO.xyplot!","page":"Home","text":"xyplot!([panel], x, y; attributes...)\n\nAdd a line to a panel instead of replacing what is there. Takes everything xyplot does.\n\nLines need not be the same length, so two spacecraft sampled differently share a panel without any fuss.\n\nSafe to call from inside a propagation or a solver callback: writes are throttled and happen on their own task, so publishing cannot slow the run feeding it. A zoom you set survives the update, where xyplot resets the view because the data underneath it changed.\n\nReturns\n\nnothing. The panel is redrawn on the dashboard.\n\nExamples\n\nxyplot(\"Altitude\",  t_a, alt_a; name = \"Sat A\")\nxyplot!(\"Altitude\", t_b, alt_b; name = \"Sat B\")\n\n\n\n\n\n","title":"EpicycleIO.xyplot!"},{"category":"function","location":"#EpicycleIO.panel!","page":"Home","text":"panel!([panel]; layout...)\npanel!([panel], layout::PlotlyBase.Layout)\n\nSet a panel's titles, axis labels, scales and limits.\n\nArguments\n\npanel: the panel to configure. Without one you get the default panel.\nlayout...: Plotly layout attributes. Nested settings are spelled with underscores, so yaxis_type reaches Plotly as yaxis.type.\n\nNotes\n\nSettings accumulate rather than replace, so calling this twice for the same axis keeps both. Limits are an ordered pair, so an axis runs backwards if you give them backwards. A Dict is not a way in — write legend_orientation = \"h\", not legend = Dict(\"orientation\" => \"h\").\n\nConfiguring a panel that does not exist yet is allowed and creates it empty, which is what lets you set a panel up before plotting into it. A name that was meant to reach an existing panel and does not earns a warning naming the panels there are.\n\nReturns\n\nnothing. The panel is republished with the new layout.\n\nExamples\n\npanel!(\"Altitude\"; xaxis_title = \"hours from epoch\", yaxis_title = \"km\", yaxis_type = \"log\")\n\npanel!(\"Decay\"; xaxis_range = [6, 0])            # runs backwards\n\n\n\n\n\n","title":"EpicycleIO.panel!"},{"category":"function","location":"#EpicycleIO.orbitview","page":"Home","text":"orbitview([panel], sat; kwargs...)\n\nDraw a spacecraft's propagated trajectory in three dimensions, with a clock you can play.\n\nThe panel joins the dashboard beside any plots, so a trajectory and the quantities describing it share one page.\n\nEverything is read from the spacecraft:\n\nthe trajectory, from sat.history\nthe epoch, from the earliest recorded sample\none coloured arc per propagation segment, from the same palette the plots use\nthe name shown on the entity, from sat.name\n\npropagate!(prop, sat, StopAt(sat, PropDurationDays(), 1.0))\n\norbitview(sat)\norbitview(\"Mission\", sat)          # name the panel\n\nArguments\n\npanel: optional leading string naming the panel.\nsat: the spacecraft. It must have been propagated — the trajectory is read from its history.\n\nKeyword arguments\n\ncolors: palette for the arcs, cycled. Defaults to the shared palette, so a trajectory and the plots of it match.\nlinewidth: arc width in pixels.\nspeed: initial playback rate, in simulated seconds per real second. :auto fits the whole trajectory into about two minutes.\n\nNotes\n\nPositions go to the browser in the inertial frame and Cesium applies the rotation to fixed itself, so the orbit holds still against the stars and the body turns beneath it. The epoch is converted to UTC on the way out, whatever scale the history was recorded in, because Cesium reads times as UTC.\n\nAn impulsive burn is marked with a point labelled with its ΔV, differenced from the velocities either side of it rather than taken from whatever commanded it.\n\nEarth is the only central body today.\n\nReturns\n\nnothing. The view is drawn on the dashboard.\n\nThrows an ArgumentError if the spacecraft has no propagated history, or if nothing in that history is long enough to draw an arc.\n\n\n\n\n\n","title":"EpicycleIO.orbitview"},{"category":"function","location":"#EpicycleIO.report","page":"Home","text":"report(path; columns...)\n\nWrite named columns to a delimited text file.\n\nThe keywords become the column headings, so they are your words rather than whatever the quantity happens to be called — someone who did not write the run has to read this. A vector-valued column expands into components, so position becomes position_1, position_2, position_3.\n\nreport(\"flight.txt\"; time = [x.jd for x in t], position = r, velocity = v)\n\nArguments\n\npath: the file to write. An existing file is overwritten.\ndelim: what separates the columns. Two spaces by default.\ncolumns...: the data, one keyword per column. Every column must be the same length.\n\nNotes\n\nColumns carry no units of their own — they are written as given, so name the keyword for what the number is (altitude_km) if the reader will need to know.\n\nValues must render on one line. An epoch does not, so convert it first — t.jd, t.mjd or t.isot — and the file then records which scale and format you meant.\n\nReturns\n\nThe path written, so it can be passed straight on to something that reads it.\n\nThrows an ArgumentError if no columns are given, if the columns differ in length, or if a value's printed form spans lines and would break a row.\n\n\n\n\n\n","title":"EpicycleIO.report"},{"category":"function","location":"#EpicycleIO.open_dashboard","page":"Home","text":"open_dashboard()\nopen_dashboard(panel, more...)\n\nOpen the dashboard in a browser tab.\n\nWith no arguments you get every panel. Naming panels opens a tab showing only those, which is how you spread twelve plots across a few windows without splitting where they live:\n\nopen_dashboard(\"Range residuals\", \"Range-rate residuals\")   # navigation, in its own tab\nopen_dashboard(\"Porkchop\")                                  # one plot, full size\n\nThe panels are still one set on one server. A scoped tab is a view of it, so a panel that several tabs show updates in all of them.\n\nArguments\n\ntitles: the panels to show. With none, the tab shows every panel.\n\nNotes\n\nOpens a tab through the operating system's usual handler. If that fails — no browser, or a headless machine — you get a warning carrying the URL rather than an error, because failing to open a window is not a reason to stop a run.\n\nReturns\n\nnothing.\n\n\n\n\n\n","title":"EpicycleIO.open_dashboard"},{"category":"section","location":"plotting_3d/#3D-Plotting","page":"3D plotting","text":"orbitview draws a propagated trajectory about the central body with a clock that can be played, paused and run at a chosen rate. The view is a panel on the same dashboard as the plots, so a trajectory and the quantities describing it are seen on one page.\n\nThis is the one part of EpicycleIO that takes a spacecraft rather than arrays. A trajectory drawn about a body requires a reference frame and an epoch, and neither is styling. The example below propagates a spacecraft and draws it, and it runs as written.\n\nusing Epicycle\nusing EpicycleIO\n\nsat = Spacecraft(state = CartesianState([7000.0, 0.0, 1300.0, 0.0, 7.35, 1.0]),\n                 time  = Time(\"2020-01-01T00:00:00.000\", UTC(), ISOT()),\n                 name  = \"Explorer\")\n\nprop = OrbitPropagator(ForceModel(PointMassGravity(earth, ())),\n                       IntegratorConfig(DP8(); abstol = 1e-12, reltol = 1e-12, dt = 60.0))\n\npropagate!(prop, sat, StopAt(sat, PropDurationDays(), 0.25))\n\n# Everything the view needs is read from the spacecraft\norbitview(\"Trajectory\", sat)","title":"3D Plotting"},{"category":"section","location":"plotting_3d/#What-Is-Read-From-the-Spacecraft","page":"3D plotting","text":"The trajectory comes from sat.history, the epoch from the earliest recorded sample, one coloured arc from each propagation segment, and the label on the entity from sat.name. The palette is the one the plots use, so a trajectory and a plot of the same run match.\n\nSegments are what make a maneuver visible. A coast, a burn and a coast leave two propagation segments in the history, and they draw in different colours, so the same maneuver appears as a colour change in the view and as a step in the plot beside it.","title":"What Is Read From the Spacecraft"},{"category":"section","location":"plotting_3d/#Maneuvers","page":"3D plotting","text":"An impulsive maneuver is recorded as a one-sample segment in the history and is marked with a point labelled with its magnitude. Nothing is passed in for it. The magnitude is the velocity change between the arcs either side of the marker, so the number on the label is read back out of the trajectory rather than taken from whatever commanded it.\n\nusing Epicycle\nusing EpicycleIO\n\nsat = Spacecraft(state = CartesianState([7000.0, 0.0, 1300.0, 0.0, 7.35, 1.0]),\n                 time  = Time(\"2020-01-01T00:00:00.000\", UTC(), ISOT()),\n                 name  = \"Explorer\")\n\nprop = OrbitPropagator(ForceModel(PointMassGravity(earth, ())),\n                       IntegratorConfig(DP8(); abstol = 1e-12, reltol = 1e-12, dt = 60.0))\n\n# Coast, burn, coast: two arcs in two colours with a labelled point between them\npropagate!(prop, sat, StopAt(sat, PropDurationDays(), 0.08))\nmaneuver!(sat, ImpulsiveManeuver(axes = VNB(), element1 = 0.20,\n                                 element2 = 0.0, element3 = 0.0))\npropagate!(prop, sat, StopAt(sat, PropDurationDays(), 0.25))\n\norbitview(\"Transfer\", sat)\n\nBecause the magnitude is differenced from the recorded velocities, a label that disagrees with the value commanded means the wrong pair of arcs was found rather than that the label is cosmetic.","title":"Maneuvers"},{"category":"section","location":"plotting_3d/#Playback-and-Appearance","page":"3D plotting","text":"Playback defaults to fitting the whole trajectory into about two minutes of real time, clamped so that a short arc is not unwatchably slow and a long one is not a blur.\n\n<!-- doc-fragment -->\n\norbitview(\"Trajectory\", sat; speed = 3600)              # one hour of flight per second\norbitview(\"Trajectory\", sat; linewidth = 3)             # arc width in pixels\norbitview(\"Trajectory\", sat; colors = [(255, 80, 80, 255), (55, 200, 255, 255)])\n\nPlay, pause, reset, slower, faster and stars sit in a bar under the view, with the clock time on the right. Holding shift while clicking slower or faster changes the rate in fives rather than doubles.","title":"Playback and Appearance"},{"category":"section","location":"plotting_3d/#The-Camera","page":"3D plotting","text":"The camera is fixed in the inertial frame. The orbit therefore holds still against the stars and the central body rotates beneath it, which is almost always what is wanted when looking at a trajectory, and it is why the view does not follow the spacecraft by default.\n\nPositions are sent to the browser in the inertial frame and Cesium applies the rotation to the fixed frame itself, once per rendered frame. The epoch is converted to UTC on the way out whatever scale the history was recorded in, because Cesium reads CZML times as UTC.","title":"The Camera"},{"category":"section","location":"plotting_3d/#One-Spacecraft-per-View","page":"3D plotting","text":"A view holds one spacecraft. A second spacecraft is given its own panel.\n\n<!-- doc-fragment -->\n\norbitview(\"Chief\", chief)\norbitview(\"Deputy\", deputy)\n\norbitview! exists and raises an error. Drawing several spacecraft in one view requires the CZML writer to merge entity sets, which it does not yet do, so it says so rather than silently drawing one and dropping the other.","title":"One Spacecraft per View"},{"category":"section","location":"plotting_3d/#Central-Bodies","page":"3D plotting","text":"Earth is currently the only supported central body. The body is taken from the frame the history was recorded in rather than assumed, and nothing in the naming or the design refers to Earth, so adding the Moon or a planet is a configuration change rather than a rewrite. The name orbitview was chosen over globe for the same reason: the first interplanetary cruise makes \"globe\" the wrong word.","title":"Central Bodies"},{"category":"section","location":"plotting_3d/#Implementation","page":"3D plotting","text":"The trajectory is written as CZML, a time-dynamic scene format, and the page hands it to Cesium to render.\n\nCesium is downloaded the first time a 3D panel appears and not before, so a dashboard of plots does not pull it. It is about 5 MB and your browser keeps it, so you pay that once rather than per session. The first 3D view on a machine with no network connection will not draw; see the dashboard page for what is cached and for how long.","title":"Implementation"}]
}
