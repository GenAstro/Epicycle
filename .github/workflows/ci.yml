name: CI
on:
  push:
  pull_request:

jobs:
  ci-pipeline:
    name: CI Pipeline - All Phases
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pages: write
      id-token: write
    
    steps:
      - uses: actions/checkout@v4
      
      - uses: julia-actions/setup-julia@v2
        with:
          version: '1.12'
          
      - name: Cache Julia artifacts and packages
        uses: actions/cache@v4
        with:
          path: |
            ~/.julia/artifacts
            ~/.julia/scratchspaces
            ~/.julia/packages
          key: ${{ runner.os }}-julia-cache-${{ hashFiles('**/Project.toml', '**/Manifest.toml') }}
          restore-keys: |
            ${{ runner.os }}-julia-cache-

      - name: Install xvfb and OpenGL dependencies
        run: sudo apt-get update && sudo apt-get install -y xorg-dev mesa-utils xvfb libgl1 freeglut3-dev libxrandr-dev libxinerama-dev libxcursor-dev libxi-dev libxext-dev

      - name: Phase 1 - Setup Environment
        run: julia --project=. ci/setup_environment.jl

      - name: Phase 2 - Build Epicycle
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: DISPLAY=:0 xvfb-run -s '-screen 0 1024x768x24' julia --project=. ci/build_epicycle.jl

      - name: Upload coverage to Codecov
        uses: codecov/codecov-action@v4
        with:
          file: ./coverage.info
          token: ${{ secrets.CODECOV_TOKEN }}
          fail_ci_if_error: false

      - name: Debug - Check docs build
        if: github.ref == 'refs/heads/main'
        run: |
          echo "=== Built docs directories ==="
          find . -name "build" -type d | grep docs